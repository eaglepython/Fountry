# Alpha Foundry Backend — Requirements
# Install: pip install -r requirements.txt

# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# Data & Math
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0

# Market Data (free, no API key)
yfinance>=0.2.37

# Machine Learning
scikit-learn>=1.4.0
hmmlearn>=0.3.0
statsmodels>=0.14.0

# HTTP (FRED + SEC EDGAR)
requests>=2.31.0
aiohttp>=3.9.0

# Agents
apscheduler>=3.10.0

# Optional: FRED API client (higher rate limits)
# fredapi>=0.5.0  # Only needed if you have a FRED_API_KEY
